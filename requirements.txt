--extra-index-url https://download.pytorch.org/whl/cu124

annotated-types==0.7.0
anyio==4.13.0
bcrypt==4.1.2
certifi==2026.5.20
cffi==2.0.0
click==8.4.0
colorama==0.4.6
cryptography==48.0.0
ecdsa==0.19.2
fastapi==0.109.0
greenlet==3.5.1
h11==0.16.0
httpcore==1.0.9
httptools==0.7.1
httpx==0.26.0
idna==3.15
iniconfig==2.3.0
packaging==26.2
passlib[bcrypt]==1.7.4
Pillow==10.4.0
pluggy==1.6.0
pyasn1==0.6.3
pycparser==3.0
pydantic==2.5.3
pydantic_core==2.14.6
pytest==7.4.4
python-dotenv==1.0.0
python-jose==3.3.0
python-multipart==0.0.6
PyYAML==6.0.1
requests==2.32.3
rsa==4.9.1
setuptools==82.0.1
six==1.17.0
sniffio==1.3.1
SQLAlchemy==2.0.25
starlette==0.35.1
typing-inspection==0.4.2
typing_extensions==4.15.0
uvicorn==0.27.0
watchfiles==1.2.0
websockets==16.0
wheel==0.47.0

# ---- AI / ML stack (Day 1) ------------------------------------------------
# Install on the laptop AI server; the Pi edge only needs opencv-python-headless,
# numpy, requests, RPi.GPIO.
numpy==1.26.4
opencv-python==4.10.0.84
scipy==1.13.1
# Person detection — YOLOv8 (Ultralytics)
ultralytics==8.2.103
# CUDA torch stack — resolved via the --extra-index-url at the top of this file.
# The +cu124 local version forces pip to pick the GPU wheel over the PyPI CPU one.
torch==2.4.0+cu124
torchvision==0.19.0+cu124
torchaudio==2.4.0+cu124
# facenet-pytorch 2.6.0 pins torch<2.3.0, which would downgrade the CUDA torch
# above on a normal install. Install it manually after `pip install -r requirements.txt`:
#   pip install --no-deps facenet-pytorch==2.6.0

# ---- Alerting (Day 2) -----------------------------------------------------
twilio==9.3.0
